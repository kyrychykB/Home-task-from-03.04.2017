<?php
define('DAYS_COUNT', 7); // Works OUTSIDE of a class definition
const MONTH_COUNT = 12;  // Works both INSIDE and OUTSIDE of a class definition

var_dump(DAYS_COUNT);
echo '<br/>';
var_dump(MONTH_COUNT);
